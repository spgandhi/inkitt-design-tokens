{
  "name": "@spgandhi/inkitt-design-tokens",
  "version": "0.0.7",
  "description": "design tokens for inkitt design system",
  "repository": "https://github.com/spgandhi/inkitt-design-tokens.git",
  "author": "Shreyans Gandhi <shreyans.p.gandhi@gmail.com>",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate-theme": "node node_modules/token-transformer src/figma-tokens --theme --themeOutputPath src/theme-tokens",
    "generate-sd": "node scripts/style-dictionary/build-style-dictionary.js",
    "build-tokens": "yarn generate-theme && yarn generate-sd",
    "build": "yarn build-tokens && cp src/index.js dist && cp package.json dist/package.json && cp src/index.d.ts dist/"
  },
  "devDependencies": {
    "style-dictionary": "^3.7.1",
    "token-transformer": "^0.0.27"
  }
}
